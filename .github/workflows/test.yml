name: "build-test"
on:
  push:
    branches:
      - s/test

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Handle event
        uses: ./
